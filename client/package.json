{
  "name": "my-frontend",
  "version": "1.0.0",
  "description": "My frontend project",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",      // Run the dev server
    "build": "react-scripts build",      // Build production files
    "test": "react-scripts test",        // Run tests
    "eject": "react-scripts eject"       // Eject config (if React)
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    // other libraries your app uses
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
    // dev tools like linters, testing libs
  },
  "author": "Your Name",
  "license": "MIT"
}
